## Summary

One or two sentences: what changed and why.

## Verification

- [ ] `hemtt check -p -e` passes
- [ ] `python3 -m unittest discover -s tools/tests` passes
- [ ] `tools/docker_test.sh` passes (required for simulation changes)
- [ ] `tools/docker_test.sh --maps` passes (required for biome changes)

## Changelog entry

One line for CHANGELOG.md, or "none".
